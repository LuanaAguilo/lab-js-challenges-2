1. Challenge 1:
  - Answer: B
  - Explanation: `foo` gets changed to `"xyz"` inside the function, so both logs show `"xyz"`


2. Challenge 2:
  - Answer: C
  - Explanation: the `a` inside the function is separate, it becomes `10` but the outer `a` stays `1`


3. Challenge 3:
  - Answer: C
  - Explanation: function declarations are hoisted, so it prints `"Hi there!"`


4. Challenge 4:
  - Answer: C
  - Explanation: `a` and `b` reference the same object, so changing one affects the other


5. Bonus - Challenge 5:
  - Answer:
  - Explanation:
